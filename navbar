export default Navbar;

// File: src/pages/Home.js
import React from "react";
import heroImg from "../assets/helpdesk-illustration.png"; // use any image

const Home = () => {
  return (
    <div className="flex flex-col md:flex-row items-center justify-center p-6">
      <div className="text-center md:text-left max-w-xl">
        <h2 className="text-4xl font-bold text-indigo-700 mb-4 animate-fade-in-up">Welcome to HelpDesk</h2>
        <p className="text-lg text-gray-700 mb-6">Your one-stop solution for customer support. Raise, track and resolve your issues with ease.</p>
        <a href="/register" className="bg-indigo-500 text-white px-6 py-2 rounded hover:bg-indigo-600 transition-all duration-300">Get Started</a>
      </div>
      <img src={heroImg} alt="Helpdesk Illustration" className="w-2/3 md:w-1/2 mt-8 md:mt-0 animate-bounce" />
    </div>
  );
};

export default Home;
